# ─────────────────────────────────────────────────────────────────────────────
# IOC Checker — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────
# Install with:
#   pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# HTTP requests library — used for all API calls
requests>=2.31.0

# Rich — terminal formatting: tables, panels, colored output
rich>=13.7.0

# Standard library modules used (no install required):
#   argparse, hashlib, ipaddress, os, re, sys, concurrent.futures, urllib.parse
